Filter and consent checkboxes — used heavily by the meeting finder.

```jsx
<CheckLabel id="open" label="Open meetings only" defaultChecked />
<CheckLabel type="radio" name="fmt" id="inperson" label="In person" />
```
